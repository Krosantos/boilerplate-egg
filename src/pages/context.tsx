import { Context } from "@/templates/Context";
import React from "react";

interface ContextPageProps {}

const ContextPage: React.FC<ContextPageProps> = (props) => {
  return <Context />;
};

export default ContextPage;
